# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react) uses [Oxc](https://oxc.rs)
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react-swc) uses [SWC](https://swc.rs/)

## React Compiler

The React Compiler is not enabled on this template because of its impact on dev & build performances. To add it, see [this documentation](https://react.dev/learn/react-compiler/installation).

## Expanding the ESLint configuration

If you are developing a production application, we recommend using TypeScript with type-aware lint rules enabled. Check out the [TS template](https://github.com/vitejs/vite/tree/main/packages/create-vite/template-react-ts) for information on how to integrate TypeScript and [`typescript-eslint`](https://typescript-eslint.io) in your project.


# Galary
A React-based image gallery with API integration, pagination, loading states, and responsive UI using Tailwind CSS.
# React Image Gallery

A simple image gallery built with **React.js** and **Tailwind CSS**. The project fetches images from the **Picsum Photos API** and displays them in a clean, interactive gallery.

## Features

* Fetch images from the Picsum Photos API
* Display images in a gallery layout
* Pagination with Previous and Next buttons
* Loading state while images are being fetched
* Image hover effects
* Open images in a new tab
* Dynamic rendering using React
* Responsive and flexible layout

## Technologies Used

* React.js
* Tailwind CSS
* JavaScript
* REST API

## React Concepts Used

* `useState`
* `useEffect`
* API integration using `fetch()`
* Conditional rendering
* Rendering lists with `.map()`
* State management
* Event handling

## API

This project uses the **Picsum Photos API** to fetch images.

## Purpose

This project was created to practice **React, API integration, Tailwind CSS, and frontend UI development**.

## Author

Built as a personal project while learning and practicing React.js.